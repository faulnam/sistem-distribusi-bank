# ================================
# BankKrut API Server Configuration
# ================================
# Copy file ini ke .env dan sesuaikan nilainya
# cp .env.example .env (Linux/Mac)
# copy .env.example .env (Windows)

# ================================
# Server Configuration
# ================================
PORT=3001
NODE_ENV=development

# ================================
# Database Configuration
# ================================
# Sesuaikan dengan konfigurasi MySQL Anda
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=db_mbanking

# ================================
# JWT Configuration
# ================================
# PENTING: Ganti JWT_SECRET dengan random string yang kuat untuk production
# Anda bisa generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h
